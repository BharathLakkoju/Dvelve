# Dvelve

> **Local-first AI research assistant powered by Ollama.** Enter a topic and get a structured, multi-source research report generated by a 5-agent AI pipeline — all running on your machine.

---

## 🚀 Quick Start

### Start Everything

```batch
start.bat
```

This opens the frontend at **http://localhost:5173** and the backend at **http://localhost:8000**.

### Manual Start

```bash
# Backend
cd backend
uvicorn main:app --port 8000 --reload

# Frontend (in a new terminal)
cd frontend
npm run dev
```

---

## ✨ Features

| Feature                                                           | Status     |
| ----------------------------------------------------------------- | ---------- |
| 5-agent pipeline (Planner → Retriever → Ranker → Writer → Critic) | ✅ MVP     |
| Live SSE streaming UI                                             | ✅ MVP     |
| Offline / mock data mode (5 topics)                               | ✅ MVP     |
| Markdown report with citations                                    | ✅ MVP     |
| Table of Contents, Sources panel                                  | ✅ MVP     |
| Research Library (history)                                        | ✅ MVP     |
| Ollama model selector                                             | ✅ MVP     |
| Quality score from Critic agent                                   | ✅ MVP     |
| Export Markdown                                                   | ✅ MVP     |
| Export PDF (requires WeasyPrint)                                  | ✅ MVP     |
| Session history (SQLite)                                          | ✅ MVP     |
| Live web retrieval (DuckDuckGo)                                   | ✅ Phase 2 |
| Vector memory (ChromaDB)                                          | ✅ Phase 4 |

---

## � Live Web Retrieval (Phase 2)

When **Offline Mode is OFF**, the Retriever agent performs real-time web searches using DuckDuckGo:

- Each sub-question's search query is sent to DuckDuckGo via `AsyncDDGS`
- Results are enriched by fetching the full page text with `httpx` + `BeautifulSoup`
- Boilerplate (nav, footer, scripts) is stripped to keep only article content
- Up to 3 sub-questions are searched concurrently to meet the depth quota
- Falls back to mock fixtures if the network is unavailable

---

## 🧠 Vector Memory (Phase 4)

All web-retrieved sources are stored in a local **ChromaDB** persistent vector database (`./chroma_db/`). On subsequent research sessions:

1. The vector store is queried first for semantically similar cached sources
2. Only the remaining quota is fetched fresh from the web
3. New sources are upserted, continuously enriching the memory store
4. The SSE `retriever` event reports `cached_count` so the UI can indicate memory hits

ChromaDB uses a local ONNX embedding model (downloaded on first run, ~30 MB). No external API needed.

---

## 🏗️ Architecture

```
backend/           ← FastAPI + 5 Python agents
├── agents/        ← planner, retriever, ranker, writer, critic
├── services/      ← ollama.py, database.py (SQLite), web_retriever.py, vector_store.py
├── routers/       ← /api/research (SSE), /api/sessions, /api/models
└── data/          ← mock_fixtures.json (5 pre-loaded topics)

frontend/          ← React 18 + Vite + TailwindCSS v3
├── src/pages/     ← Dashboard, Research, Report, Settings, Library
├── src/hooks/     ← useSSE.ts (streaming)
└── src/store/     ← useStore.ts (Zustand)
```

---

## 🤖 Offline Mock Topics

The following topics have rich pre-loaded mock data:

- **AI / Machine Learning** — McKinsey, arXiv, Ollama, Anthropic, IBM Research
- **Solar Energy / Renewables** — IEA, NREL, Bloomberg
- **Quantum Computing** — IBM Research, arXiv
- **Space Exploration** — SpaceX, NASA
- **Biotech / CRISPR** — Nature, Insilico Medicine

Any other query auto-generates plausible academic-style sources.

---

## 🔧 Connecting Ollama

1. Install [Ollama](https://ollama.com) and pull a model: `ollama pull llama3:8b`
2. Go to **Settings** in the app
3. Enter your Ollama URL (`http://localhost:11434`)
4. Click **Test Connection**
5. Toggle **Offline Mode OFF** to use live Ollama inference

---

## 📦 Export

- **Markdown** — direct download from `/api/sessions/{id}/export/markdown`
- **PDF** — requires `pip install weasyprint` on the backend

---

_Dvelve v1.1 · Built with FastAPI + React + Ollama · Phase 2 + Phase 4 complete · March 2026_
